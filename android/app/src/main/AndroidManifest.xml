<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />
    <!-- Required for push notifications on Android 13+ (API 33+) -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- Required for panic full-screen alert on locked screen (Android 10+) -->
    <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT" />
    <!-- Allows waking the screen when panic alert arrives -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <!-- Lets the app ask to be exempt from battery optimization so FCM panic
         pushes arrive while the app is killed (security-alarm use case) -->
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />

    <application
      android:name=".MainApplication"
      android:label="@string/app_name"
      android:icon="@mipmap/ic_launcher"
      android:roundIcon="@mipmap/ic_launcher"
      android:allowBackup="false"
      android:theme="@style/AppTheme"
      android:usesCleartextTraffic="${usesCleartextTraffic}"
      android:supportsRtl="true">
      <!-- Canal por defecto de las notificaciones que muestra el propio sistema
           (app cerrada, mensaje FCM con payload `notification`). El backend ya
           no lo dicta: sirve a dos apps con canales distintos, así que cada una
           declara el suyo. Debe existir en CHANNEL de NotifeeService — si no,
           Android cae a un canal genérico sin prioridad ni sonido.

           `tools:replace` es obligatorio: react-native-firebase/messaging ya
           declara este mismo meta-data con un valor vacío, y sin el override el
           merge del manifiesto falla el build. -->
      <meta-data
        android:name="com.google.firebase.messaging.default_notification_channel_id"
        android:value="general"
        tools:replace="android:value" />
      <activity
        android:name=".MainActivity"
        android:label="@string/app_name"
        android:configChanges="keyboard|keyboardHidden|orientation|screenLayout|screenSize|smallestScreenSize|uiMode"
        android:launchMode="singleTask"
        android:windowSoftInputMode="adjustResize"
        android:showWhenLocked="true"
        android:turnScreenOn="true"
        android:exported="true">
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
      </activity>
    </application>
</manifest>
